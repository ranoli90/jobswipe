Android build for the Flutter project is failing due to the Flutter tool's inability to locate the Android SDK. Despite multiple attempts to configure the environment and the Flutter tool itself, the SDK is not being detected. I have confirmed that the Android SDK is installed, and I have tried setting the ANDROID_HOME and PATH environment variables in various ways. I have also used `flutter config --android-sdk` to explicitly set the SDK path. None of these measures have resolved the issue. The project's Gradle files are correctly configured to use the SDK provided by the Flutter tool, so the problem is not in the project's code or configuration. The root cause appears to be an issue with the build environment provided by the IDE or platform, where the environment variables are not being correctly propagated to the Flutter tool. This prevents `flutter doctor` from finding the SDK and, consequently, the Android build from starting. The issue needs to be addressed at the platform level to ensure the build environment is correctly configured.